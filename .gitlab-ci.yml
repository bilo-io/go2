image: node:6
stages:
  - install_dependencies
  - build_artifact
cache:
  script: echo "caching -> ./node_modules/"
  paths: 
    - node_modules/
    - dist/
    - artifact/
 before_script:
  - node -v
  - npm -v

after_script:
  - echo "Go2 YAML => after_script executed"

install_dependencies:
  stage: install_dependencies
  script:
    - npm install -g webpack
    - npm install

build_artifact:
  stage: build_artifact
  script:
      - npm run build
      - mkdir artifact
      - mv dist/ ./artifact
      - cp server.js ./artifact
      - cp package.json ./artifact
      - cd ./artifact
      - npm install --production
      - cd ../